# BACKEND_URL is read at runtime by /api/config; one image works in every environment.
BACKEND_URL=http://localhost:8000
# Optional fallback for static-only hosting such as Vercel.
NEXT_PUBLIC_API_URL=http://localhost:8000
